pandas>=1.5.0
numpy>=1.23.0
scikit-learn>=1.1.0
requests>=2.28.0
urllib3>=1.26.0
streamlit==1.32.0
# Optional observability — install with:
#   pip install sentry-sdk prometheus-client
# The engine imports them defensively; absence is a no-op.
#
# Optional FastAPI sidecar (api.py) — install with:
#   pip install fastapi uvicorn pydantic
# Only required if you run `make api` / `uvicorn api:app`.
